# 本地开发默认可以不配置，前端会通过 Vite proxy 请求 /api。
# 如果前端和后端分开部署，可以填写后端地址，例如：
# VITE_API_BASE_URL=http://localhost:8092
VITE_API_BASE_URL=

# Vite 本地开发代理目标。后端端口不是 8092 时再改这个。
VITE_BACKEND_PROXY_TARGET=http://localhost:8092
